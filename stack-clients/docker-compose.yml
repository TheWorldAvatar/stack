services:
  stack-client:
    image: ghcr.io/theworldavatar/stack-client${IMAGE_SUFFIX}:1.41.0-7-SNAPSHOT
    secrets:
      - blazegraph_password
      - postgis_password
      - geoserver_password
    volumes:
      - ./inputs/data:/inputs/data
      - ./inputs/config:/inputs/config
