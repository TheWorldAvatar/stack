services:
  stack-client:
    image: ghcr.io/theworldavatar/stack-client${IMAGE_SUFFIX}:1.40.2-post-move-version-SNAPSHOT
    secrets:
      - blazegraph_password
      - postgis_password
      - geoserver_password
    volumes:
      - ./inputs/data:/inputs/data
      - ./inputs/config:/inputs/config
