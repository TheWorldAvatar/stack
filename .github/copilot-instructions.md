# Copilot instructions for `stack`

## Build commands

- In `stack-data-uploader`, `stack-manager`, and `stack-clients`, use `stb` to build the project. It is the local alias for `./stack.sh build` in all three folders, and it is the command that should be used when working in those directories.
